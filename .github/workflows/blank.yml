name: Hello World

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: build step
       run: echo "a demo build "


  test:  
    runs-on: ubuntu-latest
    steps:
     - name: test phase
       run: echo "running test case"


  deploy:
    runs-on: ubuntu-latest
    steps:
     - name: deploy the app
       run: echo "deploy the app"
     
       
     
  
  
  
